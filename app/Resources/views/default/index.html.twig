{% extends 'base.html.twig' %}


{% block main %}
        {% if balances is defined %}
            Your wallet balance:
            <br><br>
            <table>
                {% for key, balance in balances %}
                    <tr>
                        <td>{{ key ?? '' }}:&nbsp&nbsp</td>
                        <td>{{ balance ?? '' }}&nbsp&nbsp</td>
                        <td>Обменять</td>
                    </tr>
                {% endfor %}
            </table>
        {% endif %}
{% endblock %}




